<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Your Anime List</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Your Anime List</h1>
  <a href="/users/<%= user._id %>/animes/new">Add a New Anime</a>
  <ul>
    <% animes.forEach((anime) => { %>
      <li>
        <%= anime.title %> - <%= anime.status %>
        <% if (anime.notes && anime.notes.trim().length > 0) { %>
          - "<%= anime.notes %>"
        <% } %>
        <% if (anime.recommended) { %>
          Recommended
        <% } else { %>
          Not Recommended
        <% } %>
        <a href="/users/<%= user._id %>/animes/<%= anime._id %>/edit">Edit</a>
        <form action="/users/<%= user._id %>/animes/<%= anime._id %>?_method=DELETE" method="POST" style="display: inline;">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>
